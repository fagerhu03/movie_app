import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors (from Figma)
  static const Color primary = Color(0xFF1DB954); // Example green
  static const Color secondary = Color(0xFFE50914); // Example Netflix red
  static const Color accent = Color(0xFFFFC107); // Example gold

  // Backgrounds
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color card = Color(0xFF222222);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textDisabled = Colors.white38;

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);

  // Borders & Dividers
  static const Color border = Color(0xFF2C2C2C);
}
